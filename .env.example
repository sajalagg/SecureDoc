# Generate a 32-byte Base64 key with:
# python -c "from app.encryption.crypto import generate_key; print(encode_key(generate_key()))"
# Never commit the real value to Git.
SECUREDOC_DOCUMENT_KEY_BASE64=replace-with-a-generated-32-byte-base64-key

